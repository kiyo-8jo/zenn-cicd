name: ci

on:
    push:
        branches: [main]

jobs:
    prettier:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/Checkout@v4
            - run: npm run format:fix